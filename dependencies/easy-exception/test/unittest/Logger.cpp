#include "catch.hpp"
#include <ee/Logger.hpp>

TEST_CASE("ee::Logger") {
/*
    ee::Logger logger("MyLoggerName");

    SECTION("const std::string& getName() const noexcept") {
        REQUIRE(logger.getName() == "MyLoggerName");
    }
*/
}